{
  "name": "@fcp/constants",
  "version": "0.1.0",
  "private": true,
  "description": "Shared injection tokens and infrastructure constants used across all FCP apps and services.",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
