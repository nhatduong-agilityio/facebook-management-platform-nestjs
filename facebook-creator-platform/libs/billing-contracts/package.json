{
  "name": "@fcp/billing-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared HTTP boundary shapes for the billing service (request/response DTOs, error codes).",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
